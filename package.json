{
  "scripts": {
    "build": "esbuild js/esbuild.js --bundle --minify --loader:.png=file --loader:.woff=file --loader:.woff2=file --outdir=dist",
    "start": "esbuild js/esbuild.js --bundle --minify --loader:.png=file --loader:.woff=file --loader:.woff2=file --outdir=dist --watch"
  },
  "dependencies": {
    "@fortawesome/fontawesome-free": "^6.0.0",
    "animate.css": "^4.1.1",
    "bootstrap": "^5.1.3",
    "bootstrap-icons": "^1.8.1",
    "esbuild": "^0.14.24",
    "jquery": "^3.6.0",
    "jquery.easing": "^1.4.1",
    "lazysizes": "^5.3.2",
    "live-server": "^1.2.1",
    "owl.carousel": "^2.3.4",
    "tiny-slider": "^2.9.4",
    "wow.js": "^1.2.2"
  }
}
